spring:
  datasource:
    url: jdbc:mysql://mysql:3306/k_blog?useSSL=true&serverTimezone=UTC&connectionTimeZone=UTC&characterEncoding=utf8
    username: ${DB_USERNAME:kblog}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: ${DDL_AUTO:update}
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        jdbc:
          time_zone: UTC
    open-in-view: false

logging:
  file:
    path: /app/logs
  level:
    root: INFO
    com.kk.kblog: INFO
    org.hibernate.SQL: warn
    org.hibernate.type.descriptor.sql.BasicBinder: warn

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
  health:
    defaults:
      enabled: true

app:
  cors:
    allowed-origins: "https://blog.ksite.xin"
  admin:
    username: ${ADMIN_USERNAME:kK}
    password: ${ADMIN_PASSWORD}
  ip:
    fastapi:
      enabled: true
      url-template: ${IP_FASTAPI_URL_TEMPLATE:http://ip_resolver:9000/resolve?ip={ip}}
      connect-timeout-ms: ${IP_FASTAPI_CONNECT_TIMEOUT_MS:800}
      read-timeout-ms: ${IP_FASTAPI_READ_TIMEOUT_MS:1200}
  oss:
    ali:
      enabled: ${ALI_OSS_ENABLED:true}
      endpoint: ${ALI_OSS_ENDPOINT:oss-cn-beijing.aliyuncs.com}
      bucket: ${ALI_OSS_BUCKET:oss-kk}
      access-key-id: ${ALI_OSS_ACCESS_KEY_ID:}
      access-key-secret: ${ALI_OSS_ACCESS_KEY_SECRET:}
      prefix: ${ALI_OSS_PREFIX:kblog/}
      public-base-url: ${ALI_OSS_PUBLIC_BASE_URL:}
      presign-expire-seconds: ${ALI_OSS_PRESIGN_EXPIRE_SECONDS:600}
  ai:
    api-key: ${AI_API_KEY:}
    model: ${AI_MODEL:glm-5.3-flash}
    base-url: ${AI_BASE_URL:https://newapi.ksite.xin/v1/chat/completions}
    timeout-seconds: ${AI_TIMEOUT_SECONDS:60}
